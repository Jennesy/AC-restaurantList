<div class="container py-5">
  <div class="row">
    <div class="col-12 col-lg-10 col-xl-8 mx-auto">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><b><span class="text-major-dark">{{restaurant.name}} </span>編輯資料</b></h5>
          <hr>
          <form action="/restaurants/{{restaurant._id}}?_method=PUT" method="POST">
            <div class="form-group">
              <label for="name">餐廳名字</label><small class="text-major"> 必填</small>
              <input type="text" class="form-control" name="name" id="name" placeholder="餐廳名字"
                value="{{restaurant.name}}" required>
            </div>
            <div class="form-group">
              <label for="name_en">餐廳名字 (英文)</label>
              <input type="text" class="form-control" name="name_en" id="name_en" value="{{restaurant.name_en}}"
                placeholder="餐廳名字 (英文)">
              <small class="form-text text-muted">說明：餐廳的英文名，方便使用者搜尋。</small>
            </div>
            <div class="form-group">
              <label for="category" class="form-label">類別</label><small class="text-major"> 必填</small>
              <input class="form-control" list="datalistOptions" name='category' id="category" placeholder="選擇或輸入類別"
                value="{{restaurant.category}}" required>
              <datalist id="datalistOptions">
                <option value="中東料理">
                <option value="日本料理">
                <option value="義式餐廳">
                <option value="美式餐廳">
                <option value="酒吧">
                <option value="咖啡廳">
              </datalist>
            </div>
            <div class="form-group">
              <label for="image">照片</label>
              <input type="text" class="form-control" name="image" id="image" value="{{restaurant.image}}" placeholder="
                圖檔連結">
              <small class="form-text text-muted">說明：放上一張代表餐廳的照片吧！</small>
            </div>
            <div class="form-group">
              <label for="location">地址</label><small class="text-major"> 必填</small>
              <input type="text" class="form-control" name="location" id="location" placeholder="地址"
                value="{{restaurant.location}}" required>
            </div>
            <div class="form-group">
              <label for="phone">連絡電話</label>
              <input type="text" class="form-control" name="phone" id="phone" value="{{restaurant.phone}}"
                placeholder="02 1234 5678">
            </div>
            <div class="form-group">
              <label for="google_map">Google Map 連結</label>
              <input type="text" class="form-control" name="google_map" id="google_map"
                value="{{restaurant.google_map}}" placeholder="https://www.google.com.tw/maps/">
            </div>
            <div class="form-group">
              <label for="rating">評分</label><small class="text-major"> 必填</small>
              <input type="number" class="form-control" name="rating" id="rating" value="{{restaurant.rating}}"
                placeholder="0-5星評價" step="0.1" min="0" max="5" required>
            </div>
            <div class="form-group">
              <label for="description">簡介</label>
              <textarea type="text" class="form-control" name="description" id="description"
                value="{{restaurant.description}}" placeholder="寫下餐廳簡介" rows="3"></textarea>
            </div>
            <div class="d-flex justify-content-end">
              <a href="/restaurants/{{restaurant._id}}" class="btn btn-secondary mt-2 mr-2">取消</a>
              <button type="button" class="btn btn-major-green mt-2" data-bs-toggle="modal"
                data-bs-target="#editModal">儲存</button>
            </div>
            <!-- Modal -->
            <div class="modal fade" id="editModal" tabindex="-1" aria-labelledby="editModalLabel" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">
                  <div class="modal-body text-center mt-1">
                    <i class="fas fa-exclamation-triangle pb-4 text-major-red"></i><br>
                    確定儲存修改嗎？<br>儲存後先前資料將被覆蓋。
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary btn-sm" data-bs-dismiss="modal">取消</button>
                    <button type="submit" class="btn btn-major-green btn-sm">儲存</button>
                  </div>
                </div>
              </div>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>